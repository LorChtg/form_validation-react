{
  "name": "form-input-validation",
  "version": "1.0.0",
  "description": "A simple form with validation, made for training.",
  "main": "index.js",
  "scripts": {
    "babel": "npx babel --watch src --out-dir . --presets react-app/prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
